flask>=2.2
requests>=2.28
fast-flights>=2.0        # 构造 Google Flights tfs 查询 URL（默认数据源，单次 HTTP 抓取解析）
playwright>=1.40         # 仅「飞猪」可选源需要（驱动登录态 Chrome）；谷歌源不需要
# 可选：部分飞猪 listingsearch 响应为 br 压缩时用于解压（fliggy.py 有 gzip/zlib 兜底，通常不必装）
# brotli
